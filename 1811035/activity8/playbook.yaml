---
- name: install docker in ubuntu
  hosts: ubuntu
  roles:
    - ubuntu-docker
    - user-docker
    - file-docker

- name: install docker in centos
  hosts: centos
  roles:
    - centos-docker
    - group-docker
